name: adaptive_svg
description: Platform-adaptive SVG rendering. DOM <svg> on web, flutter_svg on native.
version: 0.1.0
homepage: https://github.com/rlch/adaptive_svg
repository: https://github.com/rlch/adaptive_svg
issue_tracker: https://github.com/rlch/adaptive_svg/issues

topics:
  - svg
  - web
  - platform
  - rendering

environment:
  sdk: ^3.11.1
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^2.0.17
  web: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
